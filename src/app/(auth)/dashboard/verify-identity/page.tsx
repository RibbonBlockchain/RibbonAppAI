"use client";

import React from "react";
import VerifyIdentity from "@/components/modal/verify-identity-modal";

const Page = () => {
  return <VerifyIdentity isOpen={true} />;
};

export default Page;
